---
title: Blog
pageable: true
per_page: 10
---


<h1>Writing</h1>
<p>My thoughts here and on social media are my own and not those of my employer or other organizations I participate in.</p>

<ul>
  <% page_articles.each_with_index do |article, i| %>
    <li>
      <h2><%= link_to article.title, article %></h2>
      <div class="article-details">
        <div class="article-date">
          <%= article.date.strftime('%Y-%m-%d') %>
        </div>
        <% if article.data[:description] %>
          <div>
            <%= article.data[:description] %>
          </div>
        <% end %>
      </div>
    </li>
  <% end %>
</ul>

<% if paginate && (prev_page || next_page) %>
  <p>Page <%= page_number %> of <%= num_pages %></p>
  <p>
  <% if prev_page %>
    <%= link_to '<i class="fa fa-angle-double-left"></i> previous',
        prev_page, :class => "pagination" %>
  <% end %>
  <% if next_page %>
    <%= link_to 'next <i class="fa fa-angle-double-right"></i>',
        next_page, :class => "pagination" %>
  <% end %>
  </p>
<% end %>

<!-- <% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>

<ul>
  <% page_articles.each_with_index do |article, i| %>
    <li>
      <h3><%= link_to article.title, article %></h3>
      <h5><%= article.date.strftime('%b %e') %></h5>
      <p><%= article.body %></p>
    </li>
  <% end %>
</ul>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %> -->
